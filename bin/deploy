#!/bin/bash

extract_jar_name() {
    lein_output=$1
    line=$(sed $lein_output -n 2p)
    echo "${line:8:${#line}-1}"
}

jar_name=$(lein do pom, jar | extract_jar_name)
echo "Deploying ${jar_name} to Clojars..."
scp pom.xml ${jar_name} clojars@clojars.org:
