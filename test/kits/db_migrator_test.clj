(ns kits.db-migrator-test
  (:use kits.db-migrator))

(set! *warn-on-reflection* false)

;;; Even though there are no tests in this file it still serves the purpose of
;;; testing that the namespace can be loaded
